// import { JobsByDate } from "@/lib/organizeJobsByDate";
// import JobSectionSingle from "./JobSectionSingle";

// export default function JobByDate({ jobs }: { jobs: JobsByDate }) {
//   const sections = [];

//   for (const date in jobs) {
//     sections.push(
//       <JobSectionSingle key={date} time={date} jobs={jobs[date]} />
//     );
//   }

//   return <div>{sections}</div>;
// }
